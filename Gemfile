source 'https://rubygems.org'
ruby '2.5.0'

if Gem::Version.new(Bundler::VERSION) < Gem::Version.new('1.16.1')
  abort "Bundler version >= 1.3.5 is required"
end

# httparty used for making requests in service clients
gem 'httparty'

# Use binding.pry for debugging
gem 'pry'
